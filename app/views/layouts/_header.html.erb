<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
             <a class="brand" href="/">Continder!</a>

                <div class="nav-collapse">
                    <ul class="nav">
                        <li>
                            <%= link_to 'About', '#' %>
                        <li>    
                            <%= link_to 'Contact',  '#' %>
                        </li>
                    </ul>
                    <ul class="nav pull-right">
                        <% if current_user %>
                            <li>Signed in as <strong style="margin-right: 15px;"><%= current_user.name %>!</strong><img style="height: 64px; width: 64px;" src="http://graph.facebook.com/<%= current_user.uid %>/picture?width=64&height=64"></li>
                            <li><%= link_to "Sign out", signout_path, id: "sign_out" %></li>
                        <% else %>
                            <li><%= link_to "Login with Facebook", "/auth/facebook", id: "sign_in" %></li>
                        <% end %>
                    </ul>
                </div>
        </div>
    </div>
</div>